name: Publish release (reproducibility bundle + checksums)

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build canonical reproducibility bundle
        run: |
          bash scripts/build_reproducibility_bundle.sh

      - name: Create GitHub release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          body_path: RELEASE_NOTES_v1.0.16.md
          files: |
            docs/reproducibility_bundle/crc_visium_boundary_reliability_v1.0.16.zip
            docs/reproducibility_bundle/CHECKSUMS.sha256
