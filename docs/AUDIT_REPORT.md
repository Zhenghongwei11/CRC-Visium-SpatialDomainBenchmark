# Reproducibility & Consistency Check: CRC Visium Domain Benchmark

**Status**: ✅ Consistency checks completed (primary endpoints pass the prespecified claim gates)
**Date**: 2026-04-22

This note summarizes what is *verified* about the repository outputs and what is *not claimed* (e.g., statistical significance when $q\ge 0.05$). It is designed to help reviewers map manuscript statements to concrete, versioned tables.

## 1. Data provenance (public sources)
The curated public GEO series used by this repository are listed in `docs/DATA_MANIFEST.tsv` (URLs + expected file sizes):
- GSE267401 (CRC Visium; local pilot)
- GSE311294 (CRC Visium; replication)
- GSE280318 (CRC Visium; replication; smaller bundle)

## 2. Core numerical claims (source-of-truth tables)
The source of truth for inferential summaries is `results/benchmarks/statistical_gate_summary.tsv`, which is generated by:
- `Rscript scripts/build_statistical_gate_summary.R`

Key takeaways from the current tables:
- **Primary (modern baseline) comparison**: BayesSpace vs `M5_stagate` (K=4/6 aggregated at the sample level).
  - Spatial coherence: BH-FDR $q = 0.0042$ (gate status = pass).
  - Marker coherence: BH-FDR $q = 0.0119$ (gate status = pass).
- **Stability gate** (multiseed ARI vs prespecified threshold 0.60):
  - One-sided stability test: BH-FDR $q = 0.0069$ but the direction-consistency criterion is not met (gate status = fail).
- **Compute feasibility (local-first)**:
  - Median wall time: 13.3 s (descriptive gate; see `results/benchmarks/runtime_memory.tsv`).

## 3. Internal consistency checks
The repository includes table/figure provenance mappings to reduce drift:
- `docs/FIGURE_PROVENANCE.tsv` (figure ↔ table lineage)
- `docs/SOURCE_DATA_MAP.tsv` (where each derived table comes from)

## 4. What this repository does *not* claim
- No claim of universal superiority across all datasets, preprocessing regimes, or hyperparameter settings beyond the fixed configuration and the evaluated GEO cohorts.
- No claim of biological “ground truth” compartments; results are reported as trade-offs under the fixed configuration.
